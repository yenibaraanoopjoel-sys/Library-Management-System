/// Environment and global runtime configuration
class AppConfig {
  static const String environment = 'development';
  static const bool enableLogging = true;
  static const int connectionTimeoutMs = 15000;
}
