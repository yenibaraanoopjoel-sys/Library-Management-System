name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # TODO: Add build, lint, and test jobs for frontend and backend
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      # TODO: Add build and test steps
